{
  "name": "@stealth/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Command-line scanner. Runs the real pipeline against live providers or a bundled sample chain.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "scan": "vite-node src/scan.ts --"
  },
  "dependencies": {
    "@stealth/core": "workspace:*",
    "@stealth/data": "workspace:*"
  }
}
